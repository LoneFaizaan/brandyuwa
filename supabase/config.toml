# Settings pushed to the hosted project with `npx supabase config push`.
# Only what is declared here is changed; everything else is left as set in the dashboard.
project_id = "brandyuwa"

[auth.email]
# Staff sign in with a 6-digit code sent by email, valid for 10 minutes
otp_length = 6
otp_expiry = 600

# Refuses to create an account for any email not in public.staff_members
[auth.hook.before_user_created]
enabled = true
uri = "pg-functions://postgres/public/hook_before_user_created"

# Supabase Auth never sends an email itself: login codes are sent only by the staff-login
# Edge Function, after it has checked the staff password.
[auth.hook.send_email]
enabled = true
uri = "pg-functions://postgres/public/hook_send_email"

# Called by the login page before anyone is logged in
[functions.staff-login]
verify_jwt = false
